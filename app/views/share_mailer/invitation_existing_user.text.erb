<%= @owner.username %> shared the address book "<%= @addressbook.displayname %>" with you (<%= @share.permission %> access).

Accept the invitation: <%= @accept_url %>

If you weren't expecting this, you can ignore this email.
